import {Meta, Story, Preview, Props} from '@storybook/addon-docs/blocks'
import { Chart } from './Chart'
import { ChartDefaultProps } from "./DefaultProps";

<Meta title="Components/WindRoseChart" component={Chart} />

const { data, columns, angles, width, height} = ChartDefaultProps;

# Chart

This is the `Chart`

```jsx
import { Chart } from "windrose-chart"
```

<Preview withSource="open">
  <Story name="Chart">
    <Chart />
  </Story>
</Preview>

## Props

<Props of={Chart} />

## Using different Prop Name

<Preview withSource="open">
  <Story name="Different Prop (soon)">
    <Chart />
  </Story>
</Preview>